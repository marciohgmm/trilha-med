# Referência local do projeto Firebase revalida-cards.
# No deploy, params/secrets do Firebase têm prioridade sobre este arquivo.
# Guia completo: docs/MERCADOPAGO_WEBHOOK_CONFIG.md
#
# IMPORTANTE: confirme a URL real após deploy:
#   firebase functions:list --project revalida-cards
# Use no Mercado Pago e em MERCADOPAGO_WEBHOOK_URL exatamente a URL publicada.

# Referência Gen2 esperada (ajuste se o list mostrar outra URL)
MERCADOPAGO_WEBHOOK_URL=https://mercadopagowebhook-i7lgy43gia-rj.a.run.app

# Produção: sempre false. Skip só no emulador (.env.revalida-cards.emulator.example)
MERCADOPAGO_WEBHOOK_SKIP_SIGNATURE=false

MERCADOPAGO_ALLOW_CHECKOUT_WITHOUT_WEBHOOK=false
