name: flutter_application_1
description: "Revalida Cards - Prova Revalida"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  file_picker: ^10.1.7
  path: ^1.9.0
  universal_html: ^2.2.4
  cupertino_icons: ^1.0.8
  local_auth: ^3.0.1
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  url_launcher: ^6.3.1
  shared_preferences: ^2.5.4
  flutter_secure_storage: ^9.2.4
  connectivity_plus: ^7.0.0
  http: ^1.6.0
  cloud_firestore: ^6.3.0
  firebase_storage: ^13.3.0
  image_picker: ^1.1.2

  # 🔥 Editor rico (VERSÕES COMPATÍVEIS)
  flutter_quill: 11.5.0
  flutter_quill_extensions: ^11.0.0

  flutter_html: ^3.0.0-beta.2
  audioplayers: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sounds/beep.mp3